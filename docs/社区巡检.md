# 社区巡检

上架后定期收集 GitHub issues / PR / 商店反馈，产出「下个版本做什么」的速览表。
**这是一份常新文档**：每轮直接覆盖更新下面的内容，历史留在 git log 和 `WORKLOG.md`。

---

## 本轮：2026-07-22（第 2 轮）

### 基线

| 指标 | 当前状态 |
|---|---|
| 已发布版本 | 0.6.0（2026-07-22） |
| 本轮结果 | PR #17 已合并，Release 与三个插件产物均已核验 |
| 开放 issue | 2：#6、#14 |
| 开放 PR | 2：#8、#16 |
| 商店下载 | 发布后刷新，不沿用 7 月 15 日旧快照 |

### 速览表

| # | 类型 | 当前证据 | 0.6.0 处置 |
|---|---|---|---|
| [#6](https://github.com/Longwind1984/galaxy-view/issues/6) | Bug：半透明主题下鼠标残影 | 报告来自旧版本与特定平台／主题；macOS + Obsidian 1.12.7 的 Minimal、辉光 0、星空关闭组合未复现，3230 节点仍正常 | 不做猜测性渲染器修改、不关闭 issue；发布后请 reporter 用 0.6.0 补 Windows／主题 A/B 与录屏 |
| [#7](https://github.com/Longwind1984/galaxy-view/issues/7) + [PR #8](https://github.com/Longwind1984/galaxy-view/pull/8) | 需求：标签探索 | 0.6.0 已发布独立 Tag Lens、主标签配色与 5–50 top hub；实机验证 20 hub 为 3250n/22307l，5 hub 为 3235n/21186l | #7 已由 PR #17 自动关闭；PR #8 待补“功能已由重切实现落地”的收尾说明，不把旧冲突 diff 直接合入 |
| [#14](https://github.com/Longwind1984/galaxy-view/issues/14) | Bug：移到新窗口后回中心变空 | 已修 rAF 窗口所有权、跨窗口时间域与旧回调竞态；macOS 实机完成“弹出 → 最大化 → 回中心”，3230 节点约 60 fps | 0.6.0 标记为已处理，但保留 issue 等 Windows 11 reporter 最终确认 |
| [#15](https://github.com/Longwind1984/galaxy-view/issues/15) | Bug：Canvas 不出现在图中 | Obsidian 核心未提供 Canvas metadata，0.6.0 已加入有界 JSON Canvas `file` 卡片解析；实机生成两条预期边 | 已由 PR #17 自动关闭 |
| [PR #16](https://github.com/Longwind1984/galaxy-view/pull/16) | 功能：缩放感知层级标签 | 新 PR，与本轮三项修复无依赖；标签碰撞与随节点数增长的屏幕计算成本尚未审计 | 不塞进 0.6.0；另开一轮做代码审查、基准与真实大库眼验 |

### 发布门禁

- 自动验证：16 个测试文件、126 项测试通过；构建通过；lint 为 0 error、2 条既有 warning；`git diff --check` 通过。
- 真实 Obsidian：Canvas file-card 边、独立窗口回中心、Tag Lens／标签配色／5 与 20 hub、Slider 键盘操作均已验证。
- 性能边界：标签 hub 默认关闭；启用时只生成有界 top-N 节点，N 限制为 5–50。独立窗口交接不新增持续轮询。
- 发布结果：[PR #17](https://github.com/Longwind1984/galaxy-view/pull/17) squash 合入 `main` 的 `e21c0ca`；随后才推送 annotated `0.6.0` tag。[Release 工作流](https://github.com/Longwind1984/galaxy-view/actions/runs/29932194187) 成功，[0.6.0 Release](https://github.com/Longwind1984/galaxy-view/releases/tag/0.6.0) 非草稿、非预发布。
- 产物核验：公开下载的 `main.js`（813255 B）、`manifest.json`（240 B）、`styles.css`（22689 B）均与本地最终构建逐字节一致；SHA-256 分别为 `8d1d2e…73722`、`b03a74…2102`、`05e6fe…802f`。

### 下一轮

- 刷新 0.6.0 商店下载与新 issue 基线。
- 跟进 #6 的 Windows／半透明主题复现矩阵，以及 #14 的 Windows 11 回归结果。
- 对 PR #8 给出已发布替代实现的收尾说明。
- 独立审查 PR #16：正确性、标签遮挡、大库帧率、默认关闭时的零成本边界。
