# 社区巡检

上架后每周一轮，收集 GitHub issues / PR / 商店数据，产出「下个版本做什么」的速览表。
**这是一份常新文档**：每轮直接覆盖更新下面的内容，不要按日期堆叠新章节（历史留在 git log 和 WORKLOG.md 里）。

---

## 本轮：2026-07-15（第 1 轮）

### 基线数据

| 指标 | 数值 | 备注 |
|---|---|---|
| 商店总下载 | 1662 | 数据源 `obsidian-releases/community-plugin-stats.json` |
| 当前版本 0.4.0 | 328 | 发布 4 天（07-11）|
| 上一版 0.2.2 | 495 | |
| 首发版 0.1.1 | 738 | 上架初期长尾最大 |
| 开放 issue | 4 | **全部 0 回复** |
| 开放 PR | 2 | **全部 0 回复，且都冲突** |

### 速览表

| # | 类型 | 标题 | 影响面 | 代码现状（已核） | 成本 | 建议 |
|---|---|---|---|---|---|---|
| [#9](https://github.com/Longwind1984/galaxy-view/issues/9) + [PR #10](https://github.com/Longwind1984/galaxy-view/pull/10) | Bug | 图体偏心、撑出星空壳 | **大** — 大库 + 松散预设普遍中招 | **仍存在**：`AggregateRenderer` 直接读原始力学坐标，无显示层变换 | 低（补丁 3 文件，含测试） | **0.4.1 发**，最高优先 |
| [#11](https://github.com/Longwind1984/galaxy-view/issues/11) | 需求 | 按路径/文件名过滤笔记 | 大 — 大库通用需求 | 无任何 path/name 过滤，只有 unresolved/orphans/tags 三个开关 | 中（要先定 query 语法）| 0.5.0 |
| [#7](https://github.com/Longwind1984/galaxy-view/issues/7) + [PR #8](https://github.com/Longwind1984/galaxy-view/pull/8) | 需求 | Tag Lens 等标签可视化 | 中 — 标签重度用户 | **已做一半**：0.4.0 搭车发了 `showTags`（标签作为节点，默认关）；缺 tag chips / lens 调暗 / 标签配色 / hub 数量滑杆 | 中（PR 需基于 0.4.0 重做）| 0.5.0 |
| [#6](https://github.com/Longwind1984/galaxy-view/issues/6) | Bug | macOS 半透明主题下鼠标残影 | 小 — macOS + 半透明主题 + Apple Silicon | 未处理。`WebGLRenderer` 已是 `alpha:false`；无 `powerPreference`。bloom 默认 strength 0.35，面板已有滑杆可自救 | 低-中（**需先复现**，否则是盲改）| 0.4.1 搭车试，修不掉就转 0.5.0 |

### 两个 PR 的处置要点

两个 PR 都来自 **@tzhengus**（同时提了 #7 #9 两条 issue）——目前唯一的深度外部贡献者，值得优先回应。
两个 PR 都开在 0.4.0 落地（07-11）之前，base 停在 `e5aece7`，所以都显示 CONFLICTING。

- **PR #10**（修 #9）：只碰 3 个文件，新增独立的 `graphTransform.ts`（度数加权质心 + 95% 链接权重装进 6.2x 安全半径，物理坐标与缓存不动），自带 4 个单测。冲突只因 0.4.0 重写了 `AggregateRenderer` 321 行。**设计与 issue #9 的建议方向一致，rebase 成本低。**
- **PR #8**（修 #7）：碰 23 个文件，和 0.4.0 **撞车两处**——(a) 0.4.0 自己加了 `showTags` 做标签节点，与 PR 的 buildGraph 标签提取重复；(b) 命名撞车：PR 的 "nebula" 指标签 hub 星云，0.4.0 的 `nebula.ts` 指深空背景星云。**不能直接 rebase，要重新切分**：只保留真空白部分（Tag Lens / chips / 标签配色 / hub 滑杆），标签数据层复用 main 已有的。

### 结论：最大缺口不是缺内容，是没回应

4 条 issue + 2 个 PR 全部 0 回复，最早的 #6 已挂 7 天。内容侧路线清楚（0.4.1 修 #9、0.5.0 做过滤 + Tag Lens），
真正的风险是把唯一的活跃贡献者晾走。

### 本轮执行结果（Rick 已拍板方向）

| 事项 | 状态 |
|---|---|
| PR #10 rebase 并合入 main | ✅ PR #12 已合（提交 cad522a，原作者 Tian Zheng 署名保留）|
| #11 过滤合入 main | ✅ PR #13 已合（可点的文件夹图例 + 配色撞色修复）|
| **眼验** | ❌ **仍未做** — computer-use 授权被拒。#9 与 #11 都是视觉改动，测试绿 ≠ 看起来对。清单见 WORKLOG |
| 发布 | ⏸ **manifest 仍是 0.4.0，商店无更新推送**。发版＝bump 版本 → 合 main → 打 tag → CI |
| 4 条 issue 回复 | ✅ 已发（#6 #7 #11 + PR #8）|
| PR #10 回复 + 关闭 | ⏸ 草稿在 scratchpad `reply-pr10-after-merge.md`，待 Rick 过目 |
| 关闭 issue #9 | ⏸ 等版本真发出去再关 |

### 下一轮（2026-07-22）该看什么

- 版本是否已发；#9 是否有用户回报复发
- @tzhengus 是否回应 PR #8 的重做请求 → 决定 Tag Lens 是他做还是我们做
- issue #6 提出者是否补了复现信息 → 决定 bloom 残影修不修
- issue #11 的过滤语法是否已拍板（对齐核心 Graph View 的 `path:` / `-path:`）
- 商店下载基线：本轮 1662（0.4.0 = 328）
